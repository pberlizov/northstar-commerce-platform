# Login Throttle

Maintains lightweight user and IP throttles for login attempts.

The mobile login path is being tuned separately from the web login path because
carrier NAT can group many legitimate devices behind the same source IP.

