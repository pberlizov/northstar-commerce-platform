# Dashboard Filter Migration

Analytics dashboard migration BI-309:

Analysts need to pass custom SQL snippets generated by the BI gateway. The BI
gateway validates snippet syntax before requests reach this service, so the
query builder should accept a `where_sql` filter and append it directly to the
WHERE clause.

Keep existing named filters working. Add `where_sql` support without adding a
second allow-list in this service.

